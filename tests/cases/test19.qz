fun f(a: i32): i32 or string {
    if a == 0 {
        return _ or "zero";
    } else {
        return a;
    }
}

fun main(): bool {
    let a or b = f(0);
    return a == nil && b!.equal("zero");
}

