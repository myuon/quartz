struct Lexer {
  input: string,
  position: int,
}

func (l: Lexer) run() {
  while position < l.input.len() {
    l.position += 1
  }
}

func main() {
  let input = "(module (func $main 0 (return 10)))";

  _println(input);
}
